package lab;

public interface MessageManager {
	//private String choice;
	/*public MessageManager(String str) {
		//choice = str;
		//getMessage(str);
		//if()
		Console s = new Console(str);
		File f = new File(str);
		if(str == "console");
			
		if (str == "file");
			
	}*/
	public void getMessage();
}
